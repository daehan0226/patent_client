import React, { FC } from 'react';
import styled from 'styled-components';

const Search: FC = () => {
    return (
        <>
            <div>Search</div>;
        </>
    );
};

export default Search;

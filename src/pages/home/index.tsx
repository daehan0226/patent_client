import React, { FC } from 'react';
import styled from 'styled-components';
import { Search } from '../../components/common';

const Home: FC = () => {
    return (
        <div>
            Home
            <Search />
        </div>
    );
};

export default Home;
